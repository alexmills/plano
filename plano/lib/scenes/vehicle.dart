/*

  Vehicle Scene
  -------------

*/

import 'package:flutter/material.dart';

class VehicleScene extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
      child: Center(
        child: Text("Vehicle"),
      ),
    );
  }
}
