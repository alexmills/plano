/*

  Navigation Scene
  ----------------

*/

import 'package:flutter/material.dart';

class NavigationScene extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pink,
      child: Center(
        child: Text("Navigation Scene"),
      ),
    );
  }
}
